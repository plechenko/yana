{
  "requires": [
    "bash",
    "apt-get"
  ],
  "params": {
    "app_dir": "/tmp/yana_demo",
    "config_file": "${param:app_dir}/config.env",
    "marker_file": "${param:app_dir}/status.txt",
    "service_port": "8080"
  },
  "vars": {
    "marker_file_content": {
      "fn": "file:read",
      "args": {
        "path": "${param:marker_file}"
      },
      "cached": true,
      "secret": true
    },
    "user": {
      "fn": "sys:user",
      "cached": true
    },
    "group": {
      "fn": "sys:group"
    },
    "owner": "${var:user}:${var:group}",
    "i": 1,
    "a": [1, 2, 3],
    "b": true,
    "s": "s",
    "empty": ""
  },
  "steps": [
    {
      "name": "Create marker file",
      "apply": "file:create",
      "args": {
        "path": "${param:marker_file}",
        "content": "STATUS=ACTIVE"
      },
      "if": [
        "${var:b}"
      ],
      "if_not": [
        "${var:empty}"
      ]
    },
    {
      "name": "Ensure application configuration exists",
      "apply": "file:write",
      "args": {
        "path": "${param:config_file}",
        "content": "marker_file=${var:marker_file_content}\nPORT=${param:service_port}\nUSER=${var:user}"
      },
      "if": "${var:b}",
      "if_not": "${var:empty}"
    }
  ]
}
